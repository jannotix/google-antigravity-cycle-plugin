{
  "mcpServers": {
    "cycle": {
      "command": "node",
      "args": ["${ANTIGRAVITY_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "CYCLE_DATA_DIR": "${ANTIGRAVITY_CYCLE_DATA:-}",
        "CYCLE_PROJECT_DIR": "${WORKSPACE_ROOT:-}"
      }
    }
  }
}
